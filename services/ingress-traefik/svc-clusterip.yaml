
apiVersion: v1
kind: Service
metadata:
  name: svc-myserver
spec:
        #externalTrafficPolicy: Local
  type: NodePort
  selector: 
    app: my-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30010
